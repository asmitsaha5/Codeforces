#include <bits/stdc++.h>
using namespace std;
void solve()
{
    vector<int> a;
    int m;
    cin >> m;
    vector<int> b;
    vector<int> c;

    for (int i = 0; i < m; i++)
    {
        cin >> a[i];
    }
    for (int i = 0; i < m; i++)
    {
        cin >> b[i];
    }
    for (int i = 0; i < m; i++)
    {
        cin >> c[i];
    }
    
}
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}